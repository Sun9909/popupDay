<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="flower.popupday.login.dao.LoginDAO">
    <!-- user_tbl -->
    <resultMap type="LoginDTO" id="LogResult">
        <result property="id" column="id"/>
        <result property="user_id" column="user_id"/>
        <result property="user_nikname" column="user_nikname"/>
        <result property="name" column="name"/>
        <result property="pwd" column="pwd"/>
        <result property="email" column="email"/>
        <result property="reg_date" column="reg_date"/>
        <result property="business_num" column="business_num"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
    </resultMap>

    <!-- sql문 중복 제거 -->
    <sql id="sr">
        <![CDATA[
        select * from user_tbl
        ]]>
    </sql>

    <insert id="insertLogin" parameterType="loginDTO">
        <![CDATA[
        insert into user_tbl values(#{id}, #{user_id}, #{user_nikname}, #{name}, #{pwd}, #{email}, now(), #{business_num}, '일반', 'active')
        ]]>
    </insert>

    <insert id="insertLogin2" parameterType="loginDTO">
        <![CDATA[
        insert into user_tbl values(#{id}, #{user_id}, #{user_nikname}, #{name}, #{pwd}, #{email}, now(), #{business_num}, '사업자', 'active')
        ]]>
    </insert>

    <select id="memberLoginCheck" parameterType="loginDTO" resultType="loginDTO">
        <include refid="sr"/>
        <where>
            user_id=#{user_id} and pwd=#{pwd}
        </where>
    </select>

    <!-- 아이디 중복 확인 쿼리 -->
    <select id="checkId" parameterType="String" resultType="boolean">
        <![CDATA[
        select count(*) > 0
        from user_tbl
        where user_id = #{user_id}
        ]]>
    </select>

    <!-- 이메일 중복 확인 쿼리 -->
    <select id="checkEmail" parameterType="String" resultType="boolean">
        <![CDATA[
        select count(*) > 0
        from user_tbl
        where email = #{email}
        ]]>
    </select>

    <!-- 닉네임 중복 확인 쿼리 -->
    <select id="checkNikname" parameterType="String" resultType="boolean">
        <![CDATA[
        select count(*) > 0
        from user_tbl
        where user_nikname = #{user_nikname}
        ]]>
    </select>

    <!-- 카카오 REST API -->
    <insert id="kakaoInsert" parameterType="LoginDTO">
        insert into user_tbl (id, user_id, user_nikname, name, pwd, email, reg_date, business_num, role, status)
        values (#{id}, #{email}, #{user_nikname}, #{name}, #{pwd}, #{email}, now(), #{business_num}, '카카오', 'active')
    </insert>

    <select id="isExistKakao" parameterType="LoginDTO" resultType="boolean">
        select if(count(*)=1, 'true','false') as result from user_tbl where user_id = #{email}
    </select>

    <update id="kakaoUpdate" parameterType="LoginDTO">
        update user_tbl set user_id=#{email}, user_nikname=#{user_nikname} where user_id=#{email}
    </update>
<!--
    <select id="getAccessToken" parameterType="String" resultType="String">
        SELECT access_token FROM user_tbl WHERE user_id=#{email}
    </select>

    <update id="delAccessToken" parameterType="String">
        UPDATE user_tbl SET access_token=null WHERE user_id=#{email}
    </update>

</mapper> -->
